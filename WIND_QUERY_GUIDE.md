# Wind 终端查询指南

## 第一步：打开 Wind 终端

1. 启动 Wind 金融终端
2. 确保已登录并有数据查询权限

## 第二步：批量查询产品价格

### 方法一：使用 Wind 批量查询功能 (推荐)

1. **打开批量查询**
   - 按快捷键 `F1` 或点击菜单：`数据` → `多维数据` → `数据浏览`
   - 或者直接在命令框输入 `WSQ` (批量实时行情)

2. **输入产品代码**
   - 将以下代码复制粘贴到查询框：

```
氧化镨钕
N35钕铁硼
氧化铈
铂
钯
铑
铌
钽
金属铍
金属铟
金属镓
锑锭
锡
白银
钼精矿
仲钨酸铵
碳化钨
五氧化二钒
铁精粉
铁矿石
钴酸锂
NCM811
氧化铝
使它隆
毒莠定
敌草快
薄荷醇
色酚
永固紫
间苯二胺
还原剂
KH-550
107胶
六氟化钨
四氧化三锰
氧氯化锆
铜杆
聚氨酯
I+G
肌苷
AES
硬脂酸
浓缩乳胶
P.O42.5水泥
木屑颗粒
电子雷管
工业炸药
高氯酸钾
风电机组
磺酸
```

3. **设置查询参数**
   - 数据类型：最新价/收盘价
   - 时间范围：最近1个交易日
   - 点击`查询`或按`Enter`

4. **导出数据**
   - 查询完成后，点击右键 → `导出到Excel`
   - 保存文件名：`wind-prices-YYYYMMDD.xlsx`
   - 保存位置：`C:\个人文件夹\github\price\data\sources\`

### 方法二：使用 Wind 代码查询

如果产品名称无法直接查询，尝试使用 Wind 代码：

| 产品名称 | Wind 代码示例 |
|----------|--------------|
| 氧化镨钕 | S5114968 (上海有色) |
| 铂 | XAUPT (国际铂金) |
| 钯 | XAUPD (国际钯金) |
| 白银 | XAGUSD (国际白银) |
| 锡 | SNIX (LME锡) |
| 铁矿石 | FEFO (大商所铁矿石) |

**查询步骤**：
1. 在 Wind 命令框输入代码
2. 按 `Enter` 查询
3. 右键 → `导出到Excel`

## 第三步：更新映射表

### 映射表文件位置

```
C:\个人文件夹\github\price\scripts\update-prices-from-excel.py
```

### 打开文件

1. 用 VS Code 或记事本打开上述文件
2. 搜索 `KEY_TO_EXCEL` (约在第 150 行)
3. 找到映射表字典

### 映射表格式说明

```python
KEY_TO_EXCEL = {
    # 格式: "数据源:price:产品名": ["Excel中的产品名1", "Excel中的产品名2"],
    
    # 示例:
    "cj:price:甲醇（长三角）": ["甲醇(华东)", "甲醇(长三角)"],
    "xyzq:price:氧化镨钕": ["氧化镨钕", "氧化镨钕(华东)"],
}
```

### 添加新映射

假设你在 Wind 中查询到的产品名称是 `氧化镨钕(国产)`，需要添加映射：

```python
# 在 KEY_TO_EXCEL 字典中添加:
"xyzq:price:氧化镨钕": ["氧化镨钕(国产)", "氧化镨钕"],
```

### 未匹配产品映射参考

以下是需要添加的映射（根据 Wind 查询结果修改右侧名称）：

```python
# ============================================================
# 待添加的映射 - 请根据 Wind 查询结果修改右侧名称
# ============================================================

# 稀土产品
"xyzq:price:氧化镨钕": ["氧化镨钕(你的Wind查询结果名称)"],
"cj:price:N35钕铁硼": ["N35钕铁硼(你的Wind查询结果名称)"],
"cj:price:氧化铈": ["氧化铈(你的Wind查询结果名称)"],

# 贵金属
"xyzq:price:铂": ["铂(你的Wind查询结果名称)"],
"xyzq:price:钯": ["钯(你的Wind查询结果名称)"],
"xyzq:price:铑": ["铑(你的Wind查询结果名称)"],
"xyzq:price:白银": ["白银(你的Wind查询结果名称)"],

# 有色金属
"xyzq:price:锡": ["锡(你的Wind查询结果名称)"],
"xyzq:price:铌": ["铌(你的Wind查询结果名称)"],
"xyzq:price:钽": ["钽(你的Wind查询结果名称)"],
"xyzq:price:金属铟": ["铟(你的Wind查询结果名称)"],
"xyzq:price:金属铍": ["铍(你的Wind查询结果名称)"],
"xyzq:price:金属镓": ["镓(你的Wind查询结果名称)"],
"xyzq:price:锑锭": ["锑锭(你的Wind查询结果名称)"],
"xyzq:price:钼精矿": ["钼精矿(你的Wind查询结果名称)"],

# 钨产品
"xyzq:price:仲钨酸铵": ["仲钨酸铵(你的Wind查询结果名称)"],
"cj:price:碳化钨": ["碳化钨(你的Wind查询结果名称)"],

# 钒/铁产品
"cj:price:五氧化二钒": ["五氧化二钒(你的Wind查询结果名称)"],
"cj:price:铁精粉": ["铁精粉(你的Wind查询结果名称)"],
"xyzq:price:铁矿石": ["铁矿石(你的Wind查询结果名称)"],

# 锂电材料
"cj:price:钴酸锂": ["钴酸锂(你的Wind查询结果名称)"],
"cj:price:NCM811": ["NCM811(你的Wind查询结果名称)"],
"xyzq:price:氧化铝": ["氧化铝(你的Wind查询结果名称)"],

# 农药产品
"cj:price:使它隆": ["使它隆(你的Wind查询结果名称)"],
"cj:price:毒莠定": ["毒莠定(你的Wind查询结果名称)"],
"cj:price:敌草快": ["敌草快(你的Wind查询结果名称)"],

# 染料/颜料
"cj:price:永固紫": ["永固紫(你的Wind查询结果名称)"],
"cj:price:色酚": ["色酚(你的Wind查询结果名称)"],
"cj:price:间苯二胺": ["间苯二胺(你的Wind查询结果名称)"],
"cj:price:还原剂": ["还原剂(你的Wind查询结果名称)"],

# 特种材料
"cj:price:KH-550": ["KH-550(你的Wind查询结果名称)"],
"cj:price:107胶": ["107胶(你的Wind查询结果名称)"],
"cj:price:六氟化钨": ["六氟化钨(你的Wind查询结果名称)"],
"cj:price:四氧化三锰": ["四氧化三锰(你的Wind查询结果名称)"],
"cj:price:氧氯化锆": ["氧氯化锆(你的Wind查询结果名称)"],
"cj:price:铜杆": ["铜杆(你的Wind查询结果名称)"],
"cj:price:聚氨酯": ["聚氨酯(你的Wind查询结果名称)"],

# 食品添加剂
"cj:price:I+G": ["I+G(你的Wind查询结果名称)"],
"cj:price:肌苷": ["肌苷(你的Wind查询结果名称)"],
"cj:price:磺酸": ["磺酸(你的Wind查询结果名称)"],

# 表面活性剂
"cj:price:AES": ["AES(你的Wind查询结果名称)"],
"cj:price:硬脂酸": ["硬脂酸(你的Wind查询结果名称)"],

# 橡胶
"cj:price:浓缩乳胶": ["浓缩乳胶(你的Wind查询结果名称)"],

# 建材
"cj:price:P.O42.5水泥": ["P.O42.5水泥(你的Wind查询结果名称)"],

# 能源
"cj:price:木屑颗粒": ["木屑颗粒(你的Wind查询结果名称)"],
"cj:price:风电机组": ["风电机组(你的Wind查询结果名称)"],

# 民爆器材
"cj:price:电子雷管": ["电子雷管(你的Wind查询结果名称)"],
"cj:price:工业炸药": ["工业炸药(你的Wind查询结果名称)"],
"cj:price:高氯酸钾": ["高氯酸钾(你的Wind查询结果名称)"],

# 香精香料
"cj:price:薄荷醇": ["薄荷醇(你的Wind查询结果名称)"],
```

## 第四步：运行更新脚本

```bash
cd C:\个人文件夹\github\price
python scripts/update-prices-from-excel.py
```

## 第五步：查看结果

脚本运行后会显示：
- 匹配产品数
- 未匹配产品数
- 匹配率

## 第六步：上传到 GitHub

```bash
cd C:\个人文件夹\github\price
update-price.bat
```

---

## 常见问题

### Q1: Wind 查询不到某些产品怎么办？

A: 尝试以下方法：
1. 使用产品代码查询（见上方代码表）
2. 简化产品名称（如"氧化镨钕"改为"镨钕"）
3. 查询相关品种（如"钴酸锂"改为"碳酸钴"）

### Q2: 映射表格式错误怎么办？

A: 注意以下几点：
1. 每行结尾要有逗号
2. 中文引号要使用英文引号 `""`
3. 列表中的名称要用 `[""]` 包裹

### Q3: 如何测试映射是否正确？

A: 
1. 先添加 1-2 个映射进行测试
2. 运行脚本查看是否匹配
3. 确认无误后再添加其他映射

### Q4: Excel 文件放在哪里？

A: 
```
C:\个人文件夹\github\price\data\sources\
```

---

## 示例：添加"氧化镨钕"映射

### 1. Wind 查询
- 在 Wind 中查询"氧化镨钕"
- 假设查询结果名称是"氧化镨钕(国产)"

### 2. 编辑映射表
打开文件：
```
C:\个人文件夹\github\price\scripts\update-prices-from-excel.py
```

找到 `KEY_TO_EXCEL` 字典，添加：
```python
"xyzq:price:氧化镨钕": ["氧化镨钕(国产)"],
```

### 3. 运行脚本
```bash
python scripts/update-prices-from-excel.py
```

### 4. 查看结果
- 如果显示"氧化镨钕 匹配成功"，说明映射正确
- 如果仍然未匹配，检查 Excel 中的实际产品名称
